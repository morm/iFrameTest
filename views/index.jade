 
doctype html
html
  head
    title= title
    link(href='https://fonts.googleapis.com/css?family=Roboto' rel='stylesheet' type='text/css')
    link(rel='stylesheet', href='/stylesheets/style.css')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    script(async src="https://www.googletagmanager.com/gtag/js?id=UA-198546757-1")
    script.
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'UA-198546757-1');

    script(type='text/javascript').
      var divSelector = 'centaurIframe';// You can your selector name here instead
      var orgId = 16;
      var centOrigin = 'https://obtest18.centaurportal.com';
      var practiceId;
      var doctorId;
      practiceId = 15;
      var sourceId=2;
      var centUserParams = ''; // You can place utm code or anything else you want to be in iframe URL
      (function() {
        var po = document.createElement('script'); po.type = 'text/javascript'; po.async = true; po.charset = 'utf-8';
        po.src = 'https://obtest18.centaurportal.com/d4w/resources/js/iframe.min.js?_=1601055215256';
        var s = document.getElementsByTagName('script')[0];
        if (s) { s.parentNode.insertBefore(po, s); }
        else { s = document.getElementsByTagName('head')[0]; s.appendChild(po); }
      })();

  body(style="font-family: 'Metrophobic', Arial, serif; font-weight: 400;")
    h1= title
    p Welcome to #{title}
    div(id="centaurIframe" style="height: 900px;")
